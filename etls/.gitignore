# NEVER commit the ETL secrets file — it holds bank-wide DB passwords, the SMTP
# password, and live LLM API keys. It is bind-mounted from the host at runtime
# (/data/apps/datascience/etls/app_settings.py -> /app/etls/app_settings.py).
app_settings.py

# Runtime working output (generated report files, caches).
attachments/
__pycache__/
*.pyc
*_log.log
*_temp.log
